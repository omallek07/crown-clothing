import INITIAL_DIRECTORY_DATA from "./directory.data";

const directoryReducer = (state = INITIAL_DIRECTORY_DATA, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default directoryReducer;
